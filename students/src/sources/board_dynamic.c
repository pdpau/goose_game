#include <stdlib.h>

#include "../headers/board_dynamic.h"

// TODO: Initializes the board, allocating the required space, and initializing also each square of it.

/**
 * Pre:
 * Post:
 *
 * Initializes the board, allocating the required space and initializing also each square of it
 *
 * @param board The board to be initialized.
 * @param rows The number of rows of the board.
 * @param columns The number of columns of the board.
 * @return SUCCESS code if the initialization was successful, ERROR code if rows are less than 1 or more than MAX_ROWS
 * or if columns is less than 2 or more than MAX_COLUMNS.
 */
int init_board(Board* board, int rows, int columns) {
    return ERROR; //copiar l'altre init_board pero "allocating the required space" ????
}